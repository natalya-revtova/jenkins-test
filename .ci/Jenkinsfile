#!groovy

node('master') {
    stage('Build') {
        sh 'ls'
        //sh 'make build'
    }
    stage('Lint') {
        sh 'make lint'
    }
    stage('Test') {
        steps {
            sh 'make test'
            sh 'make coverage'
            sh 'make clean'
        }
    }
}