#!groovy

node('master') {
    stage('Build') {
        //sh 'make build'
    }
    stage('Lint') {
        //sh 'make lint'
    }
    stage('Test') {
        steps {
            sh 'go test -cover -coverprofile=${TEST_DIR}/coverage.out'
            //sh 'make test'
            //sh 'make coverage'
            //sh 'make clean'
        }
    }
}