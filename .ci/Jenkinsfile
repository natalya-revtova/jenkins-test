#!groovy

node('master') {
    stage('Build') {
        sh 'make -sC build'
    }
    stage('Lint') {
        sh 'make lint'
    }
    stage('Test') {
        steps {
            sh 'make test'
            sh 'make coverage'
            sh 'make clean'
        }
    }
}